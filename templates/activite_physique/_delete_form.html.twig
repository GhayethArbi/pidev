{% block body %}
<form method="post" action="{{ path('app_activite_physique_delete', {'id': activite_physique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite_physique.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
{% endblock %}
