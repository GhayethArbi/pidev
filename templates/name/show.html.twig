{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recette.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ recette.category }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ recette.date ? recette.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recette.description }}</td>
            </tr>
        </tbody>
    </table>

    <button class="btn btn-outline-primary"><a href="{{ path('app_name_index') }}">back to list</a></button>

   <button class="btn btn-outline-primary"> <a href="{{ path('app_name_edit', {'id': recette.id}) }}">edit</a></button>

    {{ include('name/_delete_form.html.twig') }}
{% endblock %}
