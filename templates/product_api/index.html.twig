<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Product List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
            font-weight: bold;
            color: red; /* Set th text color to red */
        }

        tbody tr:hover {
            background-color: #f9f9f9;
        }

        .category-column {
            width: 20%;
        }

        .price-column {
            width: 15%;
        }

        .description-column {
            width: 35%;
        }

        .name-column {
            width: 20%;
        }

        .currency {
            font-weight: bold;
        }
    </style>
</head>
<body>
<h1>Product List</h1>
<table>
    <thead>
    <tr>
        <th>ID</th>
        <th >Name</th>
        <th >Descrip</th>
        <th >Category</th>
        <th >Price</th>
    </tr>
    </thead>
    <tbody>
    {% for product in products %}
        <tr>
            <td>{{ product.id }}</td>
            <td>{{ product.name }}</td>
            <td>{{ product.description}}</td>
            <td>{{ product.category }}</td>
            <td><span class="currency">$</span>{{ product.price }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>
