{% extends 'base_user.html.twig' %}

{% block content %}
    <div class="container" style="margin-top: 50px">
        <h2>Products in {{ selectedCategory.name }}</h2>
        <div id="productContainer" class="row">
            {% for product in products %}
                <div class="col-lg-3 col-md-4 col-sm-6">
                    <div class="product-item">
                        <div class="product-thumb">
                            <a href="#" class="link-to-product">
                                <img src="{{ asset('/Uploads/')}}{{ product.illustration }}" alt="{{ product.name }}" width="270" height="270" class="product-thumbnail">
                            </a>
                        </div>
                        <div class="info">
                            <b class="categories">{{ product.category }}</b>
                            <h4 class="product-title"><a href="#" class="pr-name">{{ product.name }}</a></h4>
                            <div class="price">
                                <center>
                                    <ins><span class="price-amount"><span class="currencySymbol">£</span>{{ product.price }}</span></ins>
                                </center>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
